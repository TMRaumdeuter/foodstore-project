export default function ComingSoon({ title }: { title: string }) {
  return (
    <div style={{ padding: '100px 20px', textAlign: 'center' }}>
      <h1>{title}</h1>
      <p>Trang này đang được phát triển. Vui lòng quay lại sau!</p>
    </div>
  );
}
